<% if user_signed_in? %>
<span>

 <% if current_user.user_type == "citizen"%>
  <%= link_to('Edit Profile', edit_citizen_path(current_user), :class => "route") %>
  <% else%>
  <%= link_to('Edit Profile', edit_user_registration_path(current_user), :class => "route") %>
  
  <% end %>
  </span>
  
<% else %>
  <span>
  <%= link_to('Sign up', "/citizen/find", :class => "route")  %> |
  </span>
  <span>
  <%= link_to('Login', new_user_session_path, :class => "route")  %>  
  </span>
<% end %>
