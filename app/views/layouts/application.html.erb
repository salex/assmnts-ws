<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html;charset=utf-8" >
		<title>jobs.aidt.edu - <%= params[:controller]%>/<%= params[:action]%></title>
		<%#= stylesheet_link_tag "scaffold.css"  %>
		<%= stylesheet_link_tag "screen.css", :media => 'all'  %>
		
		<%= javascript_include_tag  'jquery-1.4.4.min', 'jquery.rails','application'%>
		<script>
		  // this allows jquery to be called along with scriptaculous and YUI without any conflicts
		  // the only difference is all jquery functions should be called with $j instead of $
		  // e.g. $j('#div_id').stuff instead of $('#div_id').stuff
		  //var $j = jQuery.noConflict();
		</script>
		
		<!--[if lt IE 8]>
		  <link href="/stylesheets/ie.css" media="screen, projection" rel="stylesheet" type="text/css">
		<![endif]-->
		
		<%= csrf_meta_tag %>
	</head>
	<body>
		<div id="container">
			<div id="register" >
				<ul class="hmenu f-right">
		    <%= render 'devise/menu/registration_items' %>
		    <%= render 'devise/menu/login_items' %>
				</ul>
				<div class="f-clear"></div>
			</div>
			
			<div id="header" style="background-image:url('/images/aidt_bkg.jpg');height:96px">
				<img src='/images/logo1.jpg' class="f-left" />
				
				<% if user_signed_in? %>
				<br />
				  <span class="actions">
				      Logged in as: <%= current_user.name_full %>
				  </span>
				<% end %>
				
			</div>
			
			<div id="navbar" class="navspan ">
					<%= build_navbar %>
				
			</div>
			<div id="content">
				<%#= flash_messages %>
				<div class="flash">
				<div class="alert" style="color: red;font-size:1.3em;line-height:1.4em"><%= flash[:alert] ||= params[:alert]%> <%= error ||= params[:error]%></div>
			 
			 <div class="notice" style="color: green;font-size:1.3em;line-height:1.4em"><%= notice %></div>
			</div>
				<%= yield %>
			</div>
			<div id="footer">
				Footer
			</div>
		</div>
		<div class="f-clear"></div>
	</body>
</html>
